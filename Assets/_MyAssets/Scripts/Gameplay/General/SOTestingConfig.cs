using UnityEngine;

[CreateAssetMenu(fileName = "SOTestingConfig", menuName = "Others/SOTestingConfig")]
public class SOTestingConfig: ScriptableObject
{
    public bool IsLoadLevel = true;
    public bool IsUseJoystick = true;
}